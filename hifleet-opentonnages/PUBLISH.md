# Publish

```bash
clawhub publish ./hifleet-opentonnages --name hifleet-opentonnages --version 1.1.0
```

**v1.1.0**: Contacts on demand — list masked; **`POST {liner}/unlock`** with `product_vessel_charter` / `product_cargo_charter`; user wording **获取联系方式** (not 解锁). **`CONTACT_API.md`**, CLI `fetch-contacts`.

**v1.0.0**: Public vessel/cargo search + optional enrich-row.
